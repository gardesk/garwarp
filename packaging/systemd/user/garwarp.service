[Unit]
Description=garwarp XDG desktop portal backend
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/garwarp daemon
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
